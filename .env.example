# Copy to .env and fill values (DO NOT commit your real .env)

PORT=5000
# Production-ready URI (preferred)
MONGODB_URI=mongodb+srv://sndstore136:qeI288DUGBqzD8hn@snd-store.gfhsz.mongodb.net/all-data?retryWrites=true&w=majority&appName=SND-Store

# Cloudinary (fill with your credentials)
CLOUDINARY_CLOUD_NAME=dbh3oh0rl
CLOUDINARY_API_KEY=572764697478896
CLOUDINARY_API_SECRET=zY_4PSYs8jQKIvsCq8aaeokUCMI

# Optional
CORS_ORIGIN=https://www.shop.sndgroup.net

# Alternate Mongo configuration (use either `MONGODB_URI` or the parts below)
# The values below are derived from the `MONGODB_URI` above.
MONGODB_HOST=snd-store.gfhsz.mongodb.net
MONGODB_DB=all-data
MONGODB_USER=sndstore136
MONGODB_PASS=qeI288DUGBqzD8hn
# Set to 'true' to use mongodb+srv when building URI from parts
MONGODB_USE_SRV=true
# Optional URI query params (e.g. retryWrites=true&w=majority)
MONGODB_OPTIONS=retryWrites=true&w=majority&appName=SND-Store
MONGODB_MAX_POOL_SIZE=10
